# 🐼 萌语岛 English Island · 儿童英语学习网站设计方案 v1.1

> **给谁**：6–10 岁中国孩子 —— 三档难度（Grade 1 启蒙 / Grade 2 小学低年级 / Grade 3 中高年级），**首版做 Grade 2（6–8 岁）**，再向两头扩；家长是陪学者，不是老师
> **做什么**：每天 15 分钟，在一座「探险岛」上打通「听 → 说 → 读 → 写」四关，单词自动进记忆盒，家长手机上看得到今晚的成长报告
> **怎么做**：完全沿用参考项目 **萌码 Python（CodePanda）** 的工程哲学 —— 零构建静态站 + 全部依赖自托管 + 内容即数据 + 真跑一遍的校验脚本 + 离线优先 + Cloudflare Pages/D1 + 儿童化反馈
> **本文档状态**：**v1.1 · 已按拍板结论修订**（四项决策见 §15 决策记录），可直接进入 M0 开发

---

## 0. 一页速览

| 维度 | 结论 |
| --- | --- |
| 产品形态 | 单页 Web 应用（PWA），iPad / 学习机 / 电脑浏览器打开即用，可「添加到主屏幕」离线学 |
| 与参考项目的关系 | **同一个工程血脉，不同的产品形态**：编程站是「工作台」（三栏 IDE），英语站是「地图 + 舞台」（一屏一题的大卡片） |
| 技术栈 | 原生 JS（IIFE，零构建）+ 自托管字体/图标/音频 + Cloudflare Pages Functions + D1 + Service Worker |
| 核心引擎 | ① 音频精灵播放器 ② **本地节奏评测 + 三明治回放** ③ Leitner 记忆盒（SRS） ④ 内容校验脚本 |
| 内容规模（M4 目标） | 6 座岛 · 120 关 · 600+ 词条（三档难度）· 60 本点读绘本 · 1000+ 道题 · 全量自产 TTS 音频 |
| 差异化 | 不靠大模型聊天，靠**能自动判分的听辨训练 + 孩子的自我对比回放 + 科学的复习节奏 + 家长看得懂的周报**；中国大陆网络环境下 **100% 可用**（这一条直接来自参考项目踩过的 CDN 坑） |
| 不做 | 不做 AI 外教自由对话（成本/合规/效果都不划算，见 §8.2）、**不做云端发音评测**（已拍板：录音不出本机）、不做社交、不做广告、不收集儿童个人信息 |
| **已拍板（v1.1）** | ① 三档难度都做，首版做 6–8 岁 ② 发音评测**只做本地**、不接云端 ③ 自编主题 + 标注课标级别 ④ 部署在**现有仓库的 /en/ 子目录**（详见 §15 决策记录） |

---

## 1. 为什么做、给谁做

### 1.1 四个真实痛点

| 痛点 | 孩子的感受 | 家长的难受 |
| --- | --- | --- |
| **哑巴英语** | 不敢开口，怕读错被人笑 | 自己发音也不标准，不敢教 |
| **背了就忘** | 抄了十遍，第二天还是想不起来 | 不知道到底哪些词会了、哪些没会 |
| **没反馈** | 读完不知道对不对，很快就没劲 | 陪读一小时，说不清孩子进步在哪 |
| **坐不住** | 网课 40 分钟，5 分钟就飘走了 | 花钱买了课，孩子不肯打开 |

### 1.2 参考项目已经验证过、可以直接继承的四条结论

萌码 Python 已经验证了四件事，英语站**照抄结论、不重复踩坑**：

1. **零构建静态站 + 自托管一切**在中国大陆的网络上最稳（CDN 被拦 → 最终把 numpy 内置进仓库的教训）；
2. **内容与代码分离 + 生成脚本 + 校验脚本**，让 1318 道题的内容规模可以持续维护（「标准答案真跑一遍」）；
3. **成长系统**（等级 / 徽章 / 奖牌 / 每日任务 / 连续打卡）是真能让孩子每天回来的东西；
4. **儿童化的错误反馈**（小侦探报错）比红色 traceback 有用一百倍 —— 英语站的等价物是「小熊猫点评」，永远不出现红色的 ✗。

### 1.3 学习科学依据（每一条都对应一个具体设计）

| 原则 | 出处 | 在本产品里的落地 |
| --- | --- | --- |
| 可理解输入优先于输出（i+1） | Krashen 输入假说 | 每关**先听三遍再开口**；跟读前一定先给示范音 |
| 自然拼读是识字教学主流 | 英国 Rose Review 之后推行的 synthetic phonics | 独立「拼读岛」：字母音 → CVC → 字母组合 → 魔法 e，配拆音动画 |
| 间隔重复 | Ebbinghaus / Leitner / SM-2 | **记忆盒 5 层**（1/2/4/7/15 天），答错退回第 1 盒；多模态轮换出题 |
| 双重编码（图 + 词 + 音） | Paivio dual coding | 每个词条都有图（SVG）+ 文字 + 音频，三通道同时命中 |
| 微学习 + 固定时段 | 习惯回路 cue→routine→reward | 每天 15 分钟三段式，进门就有「今日路线」，结束必给结算奖励 |
| 自主 / 胜任 / 归属 | 自我决定理论 | 自选岛屿（自主）、徽章奖牌（胜任）、小熊猫宠物与打卡（归属） |

---

## 2. 教学法：把「每天 15 分钟」设计成一个闭环

### 2.1 主循环

```
        ┌──────────────────────── 每日 15 分钟 ────────────────────────┐
        │                                                              │
   🎒 进门            🔁 复习 3 分钟        🏝 新关 9 分钟       📖 收尾 3 分钟
   今日路线     →    记忆盒到期卡片   →   听·说·读·写·玩   →   绘本 / 对话跟读
        │             （最多 20 张）        一个小关 60–90 秒       3 句话
        │                                                              │
        └──────────────────────→ 🏁 结算：星星×3 · 经验 · 盖章 · 打卡 ──┘
                                        │
                                        ├──→ 记忆盒（新词入库、到期时间重排）
                                        └──→ 家长周报（今晚就同步）
```

**为什么「先复习再学新」**：孩子最容易在旧词上崩溃，先做会的题建立信心，再进新词；同时保证 SRS 不积压（上限 20 张，多出来的顺延到明天，避免「欠债感」劝退）。

### 2.2 一座岛的 5 个小关（所有关卡统一骨架）

| 小关 | 名称 | 干什么 | 时长 | 对应能力 |
| --- | --- | --- | --- | --- |
| 1 | 👂 **听一听** | 听音选图 / 听音选词 / 听句子判对错 | 60s | 听力辨音 |
| 2 | 🗣 **说一说** | 跟读单词 → 跟读短句，录音回放 + 点评 | 90s | 口语输出 |
| 3 | 📖 **读一读** | 拼读拆音 / 认词 / 句子填空 | 60s | 解码与认读 |
| 4 | ✍️ **写一写** | 看图拼写 / 字母拖拽 / 连词成句 | 60s | 拼写与语法 |
| 5 | 🎮 **玩一玩** | 听音抓词 / 消消乐 / 翻牌记忆（本关词的变式练习） | 60s | 巩固与心流 |

每小关 **60–90 秒**是硬约束（低于孩子的注意力衰减点），顶部有「小熊猫走路」进度条，走满即结算。

### 2.3 结算页（孩子最有成就感的一屏）

- ⭐⭐⭐ 三星制（首次全对 3 星、错 1–2 题 2 星、通关 1 星）
- ✨ 经验 +45，进度条动画推进，升级时撒花 + 升级音效
- 🔖 **盖章**：印章「啪」地盖在手账上（3D 弹跳 + 音效），这是本产品的记忆点
- 🐼 小熊猫说一句**具体**的鼓励：「今天 /æ/ 这个音读得特别准！」
- 📅 连续打卡 +1，宠物饱食度 +1

---

## 3. 内容体系：六座岛

```
                         🏝 萌语岛地图
   ┌──────────┐   ┌──────────┐   ┌──────────┐
   │ 🔤 字母岛 │ → │ 🧩 拼读岛 │ → │ 🏠 生活岛 │
   │  26 关   │   │  30 关   │   │  12 主题  │
   └──────────┘   └──────────┘   └──────────┘
                                       │
   ┌──────────┐   ┌──────────┐   ┌──────────┐
   │ 🏆 挑战岛 │ ← │ 📚 故事岛 │ ← │ 💬 句子岛 │
   │ 模拟卷+角 │   │  60 绘本  │   │  100 句型 │
   └──────────┘   └──────────┘   └──────────┘
```

| 岛 | 内容 | 规模（M4 目标） | 关键玩法 |
| --- | --- | --- | --- |
| 🔤 **字母岛** | 26 字母的名称音 + 字母音 + 首字母词（A is for apple） | 26 关 | 描红动画、字母歌、听音找字母 |
| 🧩 **拼读岛** | 短元音 CVC → 辅音组合（sh/ch/th）→ 元音组合（ai/ee/oa）→ 魔法 e | 30 关 | **拆音机**：c-a-t 三个音块滑动合并成词，配合音效 |
| 🏠 **生活岛** | 12 主题 × 40 词：颜色·数字·身体·家庭·食物·动物·衣物·玩具·学校·天气·交通·家居 | 480 词 | 主题场景大图 + 五小关 |
| 💬 **句子岛** | 100 个高频句型（I like… / Can I have…? / What's this?） | 100 关 | 句型替换练习、情景对话接龙 |
| 📚 **故事岛** | 分级点读绘本（8–16 页），每页一句话 + 3 道理解小测 | 60 本 | 点读（点哪读哪）、整本跟读、录音存「我的朗读」 |
| 🏆 **挑战岛** | 剑桥少儿 Starters/Movers 风格模拟卷（听力 + 读写） | 20 套 | 计时模拟考、成绩单、错题重做（照搬参考项目的模拟考工程） |

**三档难度怎么落（已拍板：三档都做，首版做 Grade 2）**

同一套引擎、同一批主题，差别只体现在**词条难度**与**关卡编排**上 —— 内容不用写两遍：

| 档 | 年龄 | 解锁范围 | 玩法侧重 |
| --- | --- | --- | --- |
| **Grade 1** | 4–6 岁 | 字母岛 + 生活岛前 6 个主题 | 听和说为主，几乎不要求拼写；大图大字、家长陪读 |
| **Grade 2**（首版默认） | 6–8 岁 | 完整六座岛 | 拼读 + 生活词 + 短句，四关齐全 |
| **Grade 3** | 8–10 岁 | 完整六座岛 + 挑战岛加量 | 句型、分级阅读、拼写与写作比重上升 |

实现上就是词条的 `grade: 1|2|3` 字段 + 关卡编排表按档过滤，地图左上角一个切换器。

**每个词条的字段**（`data/words-*.js`，纯数据）：

```js
{
  id: "w_apple", word: "apple", ipa: "/ˈæpl/", zh: "苹果", pos: "n.",
  theme: "food", stage: 3, level: 1,       // level 1=课标基础 2=拓展 3=挑战
  phonics: ["a", "pp", "le"],              // 拆音（拼读岛用）
  emoji: "🍎", icon: "food/apple.svg",     // 图（SVG 子集 + 自绘）
  audio: { word: "food#012", sent: "food#013" },   // 音频精灵索引，不是文件路径
  sent: { en: "I eat an apple every day.", zh: "我每天吃一个苹果。" },
  rhyme: ["apple", "happy"],               // 押韵家族
  tips: "a 在闭音节里读 /æ/，嘴巴要张大"    // 孩子和家长都看得懂的提示
}
```
---

## 4. 功能模块清单

### 4.1 孩子端（9 个）

| 模块 | 说明 | 优先级 |
| --- | --- | --- |
| 🗺 **探险地图** | 首页：六座岛 + 岛屿进度 + 今日路线入口 | M0 |
| 🏝 **关卡舞台** | 学习主界面（一屏一题），五小关串行 | M1 |
| 🔁 **今日复习** | 记忆盒到期卡片，多模态轮换出题 | M1 |
| 🗃 **我的单词本** | 5 个盒子可视化（会了的往上飘），可手动标记「这个词我还不熟」 | M1 |
| 🎤 **我的录音** | 孩子自己的朗读录音列表（**默认只存本机**），可回放、可给家长听 | M1 |
| 🧩 **拼读机** | 拖音块合成单词的独立小工具（「想读哪个词就拼给你听」） | M2 |
| 📚 **绘本架** | 点读绘本 + 整本跟读 | M2 |
| 🎮 **游戏厅** | 5 个小游戏（听音抓词 / 消消乐 / 翻牌 / 接龙 / Bingo） | M3 |
| 🏅 **成长档案** | 等级、徽章、奖牌、打卡、统计（延续参考项目 30 级 + 100 徽章 + 奖牌体系） | M1 |

### 4.2 家长端（3 个）

| 模块 | 说明 |
| --- | --- |
| 📊 **每周成长报告** | 学了哪些词、掌握率、发音得分趋势、**薄弱音素 Top5**（例如 th /θ/ 老读错）、学习时长曲线、连续天数 |
| 🗣 **今晚 5 分钟亲子任务** | 给不会英语的家长**写好台词**：「请让孩子读这 5 个词，读对就一起盖个章」，家长只需点「读对了 / 再练练」 |
| ⚙️ **设置** | 每日目标（10/15/20 分钟）、音效开关、**评测口径说明**（本地评测能做什么、不能做什么，见 §8.2）、离线音频包下载、云同步码、**单次学习时长上限（防沉迷）** |

### 4.3 系统能力

- 🏅 成长系统：经验 / 等级 / 徽章 / 奖牌 / 每日任务 / 连续打卡 / 宠物（沿用参考项目的判定式设计：check: (ctx, stats) => …）
- ☁️ 云端同步：匿名同步码 + D1 增量（换设备不丢进度）
- 📴 离线可用：Service Worker 缓存核心 + 已下载主题包，断网照常做题
- 🔗 分享页：/s/<id> 只读周报（发给爷爷奶奶看）
- 🖨 打印：单词卡 A4 打印样式（print.css）、成长周报打印

---

## 5. 关键界面设计

### 5.1 探险地图（首页）

```
┌─────────────────────────────────────────────────────────────┐
│ 🐼 萌语岛        ⭐ 128   🔥 连续 7 天      🗃 复习 12   👤 小满 │
├─────────────────────────────────────────────────────────────┤
│   ┌─── 今日路线 ─────────────────────────────────────────┐   │
│   │ ① 复习 12 张卡  ② 生活岛·食物 第 3 关  ③ 绘本《My Cat》│   │
│   │                          [ ▶ 开始今天 ]               │   │
│   └───────────────────────────────────────────────────────┘   │
│                                                             │
│      🔤 字母岛 ✅ 26/26          🧩 拼读岛 🔓 18/30           │
│             ╲                        ╱                      │
│              ╲  🏠 生活岛 🔓 3/12 ← 你在这里                 │
│               ╲                    ╱                        │
│      💬 句子岛 🔒 ─ 📚 故事岛 🔒 ─ 🏆 挑战岛 🔒              │
│                                                             │
│   🐼「小满，昨天学的 12 个词，今天该见一面啦～」              │
└─────────────────────────────────────────────────────────────┘
```

- 岛屿是**手绘地图上的节点**，未解锁的岛是灰色剪影 + 🔒；点一下显示「再学 8 关就能上岛」
- 地图可左右拖动（iPad 横向），节点有轻微呼吸动画
- 顶部四个角标就是孩子的全部状态，一屏看得完

### 5.2 关卡舞台（学习主界面，以 iPad 横屏 1024×768 为主设计目标）

```
┌─────────────────────────────────────────────────────────────┐
│ ← 退出      小关 2/5 · 说一说        🐼━━━━●━━━━━━━━  32%     │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│                       ┌──────────┐                          │
│                       │    🍎    │  ← 大图 240px（SVG）      │
│                       └──────────┘                          │
│                         apple                               │
│                      /ˈæpl/   苹果                           │
│                                                             │
│          🔊 听一听（慢速）      🔊 听一听（常速）             │
│                                                             │
│               ┌───────────────────────────┐                 │
│               │   🎤 长按说话（3 秒）      │                 │
│               └───────────────────────────┘                 │
│               ▁▃▅▇▅▃▁  ← 录音波形实时反馈                    │
│                                                             │
│               🐼「读得很完整！再来一次会更好」                │
│               [ 再试一次 ]            [ 下一个 → ]            │
└─────────────────────────────────────────────────────────────┘
```

**交互要点**

- 触控目标全部 ≥ 64×64px，主按钮 ≥ 88px 高，字号 ≥ 18px（低龄手指与视力）
- **朗读优先**：所有题干都有喇叭按钮，不识字的孩子也能独立完成听力题
- 录音时显示实时波形（孩子看得见「我在说话」）；说完自动回放一遍原音 + 一遍自己的声音（**对比本身就是核心学习动作**）
- 反馈永远**先肯定再建议**（见 §6.5）
- 每屏只有一个主动作，绝不出现两个同样重要的按钮

### 5.3 今日复习（记忆盒）

```
┌─────────────────────────────────────────────────────────────┐
│ 🗃 今日复习     12 张卡      盒①3 ②4 ③3 ④1 ⑤1               │
├─────────────────────────────────────────────────────────────┤
│    🔊 [ 播放单词 ]                                          │
│                                                             │
│       ① 🍎 apple   ② 🐱 cat    ③ 🚌 bus    ④ ☀️ sun         │
│       ⑤ 🐟 fish    ⑥ 🎩 hat    ⑦ 🥛 milk   ⑧ 🐶 dog         │
│                                                             │
│   🐼「听到哪个词，就点它的图」                                │
└─────────────────────────────────────────────────────────────┘
```

- 同一张卡**每次出现的题型不同**（听音选图 / 看图选词 / 拼写 / 跟读），避免「背位置」
- 答对：卡片「叮」地飞进盒子（飞入动画）；答错：卡片温和地晃一下，**不扣分**，只在结算里说「这 2 个词明天再见」

### 5.4 家长报告

```
┌─────────────────────────────────────────────────────────────┐
│ 📊 小满的英语周报                     9 月 1 日 – 9 月 7 日   │
├─────────────────────────────────────────────────────────────┤
│  学习 5 天 · 累计 78 分钟 · 新词 42 个 · 复习 96 次           │
│  ▁▃▅▇▅▃▁  周一 ~ 周日 时长曲线                              │
│                                                             │
│  ✅ 已经掌握 36 个   🟡 快会了 5 个   🔴 还要多听 1 个        │
│                                                             │
│  🎤 发音得分趋势   62 → 71 → 78  ↑                           │
│  ⚠️ 需要多练的音：th /θ/（think、three）· 长音 /iː/（sheep）  │
│                                                             │
│  🗣 今晚 5 分钟亲子任务                                       │
│     请孩子读这 5 个词：apple · cat · three · sheep · milk     │
│     ✅ 读对了    🔁 再练练   （家长只需点一下）                │
└─────────────────────────────────────────────────────────────┘
```

---

## 6. 视觉设计系统

### 6.1 美学方向：**旅行手账 × 丝网印刷贴纸（Risograph Travel Journal）**

刻意**不做**「紫色渐变 + 白底卡片 + 圆角阴影」的通用 SaaS 儿童风（参考项目是马卡龙渐变风，英语站要一眼区分开）。选这个方向的三个理由：

1. **手账 = 探险与收集**，天然契合「岛屿 + 盖章 + 打卡 + 徽章」的产品内核；
2. **丝网印刷的有限色板 + 厚描边 + 套色错位**，在低龄界面里可读性反而更高（颜色少 = 语义清楚）；
3. **纯 CSS/SVG 就能实现**（mix-blend-mode: multiply、SVG 噪点滤镜、双层偏移阴影），零素材成本、零第三方依赖。

### 6.2 色板（5 色，够用且不花）

| 角色 | 色值 | 用途 |
| --- | --- | --- |
| 宣纸底 | #F7EFE0 | 页面底色（叠 4% 纸纹噪点） |
| 油墨黑 | #241E19 | 描边、正文（不用纯黑 —— 纸上没有纯黑） |
| 番茄红 | #E4572E | 印章、重点、错误（必配 ✗ 图标，不只靠颜色） |
| 芥末黄 | #F2B33D | 星星、经验条、高亮 |
| 湖水蓝绿 | #2E9E8F | 正确、主按钮（必配 ✓ 图标） |
| 紫罗兰 | #6A4C93 | 次要强调、夜间模式主色 |
| 夜色底 | #1A1714 | 夜间模式（护眼，按时间自动切换，可手动覆盖） |

### 6.3 字体（全部自托管，含授权）

| 用途 | 字体 | 授权 | 为什么是它 |
| --- | --- | --- | --- |
| 英文正文 / 单词 / 题干 | **Andika** | SIL OFL | SIL 为**识字初学者**设计的字体（官网原文：a font family designed for literacy use and beginning readers），字形清晰、易混字母（b/d、p/q）区分度高 |
| 英文标题 / 数字 | **Baloo 2** | SIL OFL | 圆润厚重的展示体，贴纸感强，与描边风格契合 |
| 音标 IPA | 交给 Andika 的字符覆盖 | SIL OFL | 音标不换字体，避免方框；/θ/ /ð/ /ʃ/ /ŋ/ 是重点保障字符，上线前用脚本逐字符校验 |
| 中文 | 系统苹方/思源兜底；标题可选**站酷快乐体**子集 | 免费商用 | 中文全量字体太大，采用**按需子集化**（只打包站内出现的汉字，预计 < 400KB） |

**字体加载策略**：woff2 + font-display: swap + 只打包用到的字符子集；vendor/fonts/ 走 immutable 强缓存（沿用参考项目 _headers 策略）。

### 6.4 形状与动效语言

- **形状**：粗描边 3px + 大圆角 16–24px + 贴纸白边（box-shadow: 0 0 0 6px #fff, 0 0 0 8px #241E19）+ 轻微旋转（-2°~2°，营造手贴感）
- **质感**：纸纹（SVG feTurbulence 生成的噪点，opacity: .04）；印章用 mix-blend-mode: multiply 做油墨叠印
- **动效清单**（都走 CSS，必要时才用 rAF）：

| 动作 | 效果 | 时长 |
| --- | --- | --- |
| 卡片翻面 | rotateY 3D 翻转 | 320ms |
| 盖章 | scale(1.6) rotate(-8deg) → 落定 + 闷响音效 | 220ms |
| 答对 | 卡片上浮 + 星星迸出 + 上行三音 | 400ms |
| 答错 | 卡片左右轻晃 2 次（**不变红、不震动**）+ 温和下行两音 | 300ms |
| 升级 | 撒花（复用 confetti.min.js）+ 琶音 | 900ms |
| 小熊猫反应 | emoji 弹跳 / 歪头（不引入逐帧动画资源） | 300ms |

- **音效**：全部用 **Web Audio 合成**（延续参考项目 audio-confetti.js 的零依赖思路）：答对上行三音、答错温和两音、盖章闷响、升级琶音、翻卡「沙」。提供一键「关闭音效」。

### 6.5 反馈文案原则（儿童产品的良心所在）

| 场景 | ❌ 不要 | ✅ 要 |
| --- | --- | --- |
| 答错 | 「错误！正确答案是 apple」 | 🐼「差一点点～再听一次：🍎 apple」 |
| 发音一般 | 「62 分，不及格」 | 🐼「读得很完整！/æ/ 的嘴巴再张大一点点就更好啦」 |
| 连续错 3 次 | 一直让孩子错下去 | 自动给出正确答案 + 示范音，说「这个词先记在小本子上，明天再见它」 |
| 中途退出 | 「确定要放弃吗？」 | 「今天的进度已经存好啦，明天接着来 🐼」 |

**永远不出现**：红色叉号、倒计时压力、「你输了」、排行榜垫底。
---

## 7. 技术架构

### 7.1 继承参考项目的 12 条工程铁律

| # | 铁律 | 来源（参考项目的真实教训） |
| --- | --- | --- |
| 1 | **零构建**：原生 JS + IIFE 挂 window，改完直接部署 | 整个项目无 npm/打包，Cloudflare Pages 直接托管 |
| 2 | **顶层 const 不挂 window** → 一律用 typeof X !== "undefined" 检测 | cloud.js 拉取后界面不刷新的坑 |
| 3 | **一切依赖自托管**（字体/图标/音频/库），不信任任何第三方 CDN | jsDelivr 从 HTTPS 页面被拦，numpy 最终内置 11.9MB |
| 4 | **内容即数据**：data/*.js 纯数据 + 懒加载 + 版本号 ?v=YYYYMMDDx | lessons.js / exercises.js / examples-lib.js 的模式 |
| 5 | **生成脚本 + 校验脚本**：内容必须「真跑一遍」才准入库 | 1318 道题的期望输出是真跑参考答案得到的 |
| 6 | **离线优先**：本地存储是主存储，联网后增量上云 | localStorage 为主 + 3 秒防抖增量推送 |
| 7 | **每行数据带 account_id**：主键以账号为作用域 + rev 单调版本 + 软删除墓碑 + LWW | 第二个账号会覆盖第一个账号数据的血案 |
| 8 | **匿名同步码**身份（只存 hash + 可选 PIN），不收集儿童个人信息 | accounts 表只有 code_hash |
| 9 | **多小伙伴档案**：多孩各自独立进度 | profiles 表 + __档案id 存储命名空间 |
| 10 | **儿童化反馈**：把错误翻译成人话 + 给出下一步 | 小侦探报错（17 类中文诊断） |
| 11 | **Playwright e2e + 内容校验**，改动必须过断言 | e2e_learn_v2.mjs 61 项断言 |
| 12 | **改 css/js 必改版本号**（CDN 默认 4 小时缓存会盖掉 _headers） | README 里用血写的注意事项 |

### 7.2 与参考项目的三处**刻意不同**

| 项 | 参考项目（编程站） | 英语站 | 原因 |
| --- | --- | --- | --- |
| 界面形态 | 三栏工作台（IDE 形态） | **地图 + 舞台**（一屏一题） | 编程需要同时看题/代码/输出；英语学习需要**注意力聚焦**，多栏会分散 |
| COOP/COEP | 必须开（SharedArrayBuffer 跑 Pyodide） | **不开** | 没有 SAB 需求；开了反而限制跨域资源与 Service Worker 行为 |
| 存储 | localStorage 为主 | **localStorage（小）+ IndexedDB（录音 / SRS / 离线包索引）** | 录音与记忆盒数据量远超 localStorage 的 5MB 上限 |

### 7.3 技术栈清单

```
前端   原生 ES2020（无框架、无构建）· CSS 变量 + Grid/Flex · Web Audio · MediaRecorder
       Web Speech API（TTS 兜底）· Service Worker + Web App Manifest（PWA）
内容   静态 JS 数据文件（词库/关卡/绘本/拼读表）+ JSON 音频索引 + SVG 图标 + Opus/M4A 音频精灵
后端   Cloudflare Pages（静态托管）+ Pages Functions（/api/*）+ D1（SQLite）
运维   wrangler pages deploy · tools/dev_server.py（本地预览）· Playwright e2e
```

---

## 8. 三个关键技术方案

### 8.1 音频：**自托管音频精灵是唯一真相源，TTS 只做兜底**

**为什么不能只靠 speechSynthesis**：它的音色取决于用户设备（macOS 的 Samantha、Windows 的 Zira 各不相同），同一课在不同设备上听起来完全不一样；部分平台/浏览器依赖**网络音色**，取不到就完全没有声音 —— 对一个「靠听」的产品是致命的。参考项目已经教过一次：**能自托管的东西，绝不留在线依赖**。

**方案：构建期合成 + 音频精灵（Audio Sprite）**

```
tools/gen_audio.py
   词表 data/words-*.js
      ↓  批量合成（构建期用 edge-tts 在线合成；或用 Piper/Kokoro 本地模型，完全离线）
   片段 wav → ffmpeg 统一转码 → 拼接
      ↓
   assets/audio/food.opus   （一个主题一个文件，含该主题全部单词 + 例句）
   assets/audio/food.m4a    （iOS 老版本兜底格式）
   assets/audio/food.json   { "012": [start, dur], "013": [start, dur], … }
      ↓
tools/check_audio.py   逐条校验：文件存在 / 时长 0.3–6s / 非静音 / 索引与音频一一对应
```

**播放器**：一个 audio 元素 + currentTime = start 定位播放；切词 = 改 currentTime（无新请求、无延迟）。慢速模式 = playbackRate 0.75，**不需要第二份文件**。

**体积预算**（这是该方案能成立的关键）：

| 内容 | 数量 | 单条时长 | Opus 24kbps | 合计 |
| --- | --- | --- | --- | --- |
| 单词 | 600 | 1.2s | ~3.6KB | ~2.2MB |
| 例句 | 600 | 3.5s | ~10KB | ~6MB |
| 绘本 | 60 本 × 12 页 | 4s | ~12KB | ~2.9MB |
| **合计** | | | | **≈ 11MB（全站音频）** |

按主题分包（每包 300–500KB），**只下载孩子正在学的主题**；家长可在设置里「下载全部音频包」用于离线。相比 1200 个独立 mp3 文件（每个还有 HTTP 开销与首字节延迟），音频精灵在**体积、请求数、播放延迟**三项上全面胜出。

**兜底链**：音频精灵 → speechSynthesis（系统本地音色）→ 显示音标 + 提示「请点上面的喇叭再听一次」。三级降级，**任何情况下不会出现「没声音」的死界面**。

### 8.2 发音评测：**只做本地（已拍板），把「音准」训练挪到能自动判分的地方**

这是全项目**最容易吹牛翻车**的地方，所以设计上直接把能力边界摆到台面上。

**已拍板的取舍**：不接云端发音评测。三条硬约束 —— ① 录音不出本机（儿童隐私红线，见 §12.3）；② 零成本、零 API key、零运维，全家人谁都能用；③ 完全离线可用，回老家、坐高铁都能学。**代价是拿不到音素级的音准分**，所以真正的设计功夫不在评测，而在于：*在不评测音准的前提下，怎么让孩子把音读准*。

顺带说明：**浏览器内置的 SpeechRecognition 也一并排除** —— MDN 写得很清楚，Chrome 的实现走服务器识别、音频要上传、离线不可用（Firefox 不支持），与「录音不出本机」直接冲突，而且它给的也只是词级结果，性价比不划算。

**本地评测（Speech-Rhythm）能做什么、不能做什么**

| 维度 | 怎么算 | 能不能做 |
| --- | --- | --- |
| 读没读 | RMS 过阈值的时长占比（简易 VAD） | ✅ |
| 读完整没有 | 音节峰计数 vs 参考音的音节数（apple=2、banana=3） | ✅ |
| 节奏像不像 | 录音与示范音的**能量包络做归一化互相关** →「很像 / 有点赶 / 拖了一点」 | ✅ |
| 快慢与时长 | 总时长 ÷ 示范音时长（0.6–1.6 视为正常） | ✅ |
| 声音够不够大 | RMS 峰值与平均值 —— 低龄孩子最常见的问题就是声音太小 | ✅ |
| 停顿流利度 | 句中静音段的数量与长度 | ✅ |
| **音准（这个音读得对不对）** | —— | ❌ **做不到，绝不假装做得到** |

**拿不到音准，就用四件事补上（这才是本节真正的设计）**

| # | 补偿设计 | 为什么有效 |
| --- | --- | --- |
| 1 | 🎧 **三明治回放**：示范音 → 孩子自己的录音 → 再听一遍示范音 | 语言学习里最经典的自纠手段（self-monitoring）。孩子**自己听得出来**差别，比任何分数都管用 —— 而且纯本地、零成本 |
| 2 | 👂 **把音准训练挪到听力端**：最小对立对听辨（ship/sheep · think/sink · light/right · cat/cut · three/tree） | **听力是可以自动判分的**，而且感知先于产出：能听出区别，才可能读得出区别。每个易错音配一组听辨题 + 专属徽章 |
| 3 | 👄 **口型与发音方法提示**：每个易错音素一张口型示意图 + 一句人话（「舌尖轻轻放在上下齿之间，像这样 /θ/」） | 不判断对错，只负责**教怎么做**；静态 SVG + 轻动画，零素材成本 |
| 4 | 👨‍👩‍👧 **人工兜底**：家长亲子任务里「我听你读」，点一下「读对了 / 再练练」 | 最可靠的真评分，还顺带把亲子陪伴做进产品里（见 §5.4） |

**界面上的诚实**：不显示「发音 62 分」这类伪精确数字，只显示三个**可验证**的信号 —— `读完整了 ✓` `节奏很像 ✓` `声音再大一点点`。**永远不出现假的音准分。**

**架构预留**（不排除以后加，但 v1 绝不做）：评测器做成注册表，对外只有 `Speech.grade(blob, { text })` 一个接口，内部是 `rhythm` 实现；将来若要接云端，只需新增一个实现并在家长设置里显式开启，**UI 与数据模型都不用改**。

**隐私红线（儿童产品不可让步）**

1. 录音**只存本机**（IndexedDB），不上传、不入云同步，**服务端没有任何音频字段**；
2. 不提供录音上传与分享（想听孩子朗读，就在同一台设备上打开「🎤 我的录音」）；
3. 不做声纹留存、不做任何形式的儿童身份识别；
4. 遵循《儿童个人信息网络保护规定》的思路：**最小必要 + 家长知情**。

### 8.3 记忆盒（SRS）：Leitner 5 盒 + 多模态出题

```
   答对 → 晋级                答错 → 退回第 1 盒（不惩罚，只重排）
   ① 盒：1 天后见   ② 2 天   ③ 4 天   ④ 7 天   ⑤ 15 天 → 🎓 毕业（进「已掌握」）
```

- **入门简单可解释**：先用 Leitner（5 盒 + 固定间隔），孩子和家长都能理解「它在第 3 个盒子」；M3 之后可选叠加 SM-2 lite（ease 因子 0.8–2.5，按反应时长微调），但**默认不开**（复杂算法对 6 岁孩子没有额外价值）；
- **多模态轮换**：同一张卡在复习队列里按顺序轮换出题 —— 听音选图 → 看图选词 → 拼写 → 跟读；不同题型命中不同记忆通道，效果远好于单一闪卡；
- **节流保护**：每日复习上限 20 张，超出顺延；连续答错 3 次的词自动进「重点关注」，次日**降低难度**再出现（先给听力题，再给拼写题）；
- **数据落点**：IndexedDB（本机主存储）→ 增量同步到 D1 的 srs_cards 表。

---

## 9. 数据模型与云同步

**沿用参考项目 schema.sql 的全部设计要点**（account_id 作用域 / rev / 软删除 / rate 限流），新增英语站专属表：

```sql
-- 账号与档案：与参考项目同构（只存 code_hash，不收集个人信息）
accounts(id, code_hash, pin_hash, nickname, avatar, rev, created_at, last_seen)
profiles(id, account_id, name, emoji, grade, daily_goal, created_at, updated_at, deleted, rev,
         PRIMARY KEY(account_id, id))

-- 记忆盒（英语站的核心业务表）
srs_cards(account_id, profile_id, item_id, kind,     -- kind: word | sentence | reader | phonics
          box, due_at, streak, lapses, last_result, ease,
          updated_at, deleted, rev,
          PRIMARY KEY(account_id, profile_id, item_id))

-- 进度与成长（等级/徽章/奖牌/每日任务，整体 JSON，与参考项目一致）
progress(account_id, profile_id, stats_json, updated_at, rev, PRIMARY KEY(account_id, profile_id))

-- 按天的学习记录（家长周报要按天查，故单独建表，避免拉全量 stats）
daily(account_id, profile_id, date, minutes, new_words, reviews,
      speak_count, speak_seconds, updated_at, rev,
      PRIMARY KEY(account_id, profile_id, date))

-- 没有 recordings 表：录音只存本机 IndexedDB，服务端没有任何音频字段（已拍板：不接云端评测）
-- 所以「口语」这一块同步的只是练习次数与时长，不涉及音频内容本身

-- 公开分享（周报只读页，照搬参考项目）
shares(id, account_id, kind, profile_id, title, content, created_at, views, revoked)
rate(k, w, n)   -- 滑动窗口限流，挡暴力猜码
```

**词库、关卡、绘本、音频全部是静态资源，不入库** —— 这是刻意的：D1 免费额度是每天 500 万行读 / 10 万行写，把内容放进数据库会白白烧掉读额度（参考项目已经因为「选 D1 而不是 KV」做过一轮权衡）。

**同步协议**（与参考项目一致）：

```
GET  /api/sync?code=&pin=&since=<rev>   → 增量拉取（rev 账号级单调，不受设备时钟影响）
POST /api/sync                          → 批量推送（按行 updated_at 做 LWW，删除走墓碑）
POST /api/account                       → create / login / setpin / rotate
```
---

## 10. 目录结构（草案）

```
en/                                  ← 已拍板：现有仓库的子目录，线上 mian.lihaiyang.net/en/
├── index.html                       页面骨架（地图 / 舞台 / 复习 / 档案 / 家长 / 弹窗）
├── css/
│   ├── style.css                    设计系统 + 全部样式（含夜间模式）
│   └── print.css                    单词卡与周报的 A4 打印样式
├── js/
│   ├── app.js                       应用主流程 + hash 路由 + 首页地图
│   ├── stage.js                     🏝 关卡舞台：题型渲染器注册表（listen/speak/read/write/play）
│   ├── player.js                    🔊 音频精灵播放器（含三级降级）
│   ├── speech.js                    🎤 录音 + 本地节奏评测 + 三明治回放 + 波形
│   ├── srs.js                       🗃 记忆盒（Leitner + 多模态出题）
│   ├── progress.js                  🏅 成长档案（等级/徽章/奖牌/每日任务/打卡）
│   ├── cloud.js                     ☁️ D1 增量同步
│   ├── store.js                     💾 存储层（localStorage + IndexedDB 统一封装）
│   ├── ui.js                        通用 UI（贴纸卡 / 印章 / 撒花 / Toast / 小熊猫点评）
│   └── audio-fx.js                  🎵 Web Audio 音效合成（零依赖）
├── data/                            纯数据，懒加载 + 版本号
│   ├── words-food.js …              词库（按主题分包）
│   ├── phonics.js                   字母音 / 拼读规则表
│   ├── lessons.js                   关卡编排（只引用 word id，不重复存词）
│   └── readers.js                   绘本内容
├── assets/
│   ├── fonts/                       Andika / Baloo 2 子集（woff2）
│   ├── icons/                       OpenMoji 子集 + 自绘 SVG（按主题分包）
│   └── audio/                       音频精灵（*.opus / *.m4a / *.json）
├── sw.js  manifest.webmanifest      PWA 离线
├── functions/api/en/                英语站同步接口（复用 functions/api/_utils.js 与既有 D1 binding）
├── schema-en.sql                    英语站新增表（追加进同一个 mian-db，不动原有表）
├── _headers  wrangler.toml  README.md
└── tools/
    ├── gen_words.py                 词表 CSV → data/words-*.js（去重 + 字段校验）
    ├── gen_audio.py                 批量合成 → 音频精灵 + 索引
    ├── check_content.py             内容一致性校验（见 §11）
    ├── check_audio.py               音频校验（存在 / 时长 / 非静音 / 索引对齐）
    ├── check_fonts.py               IPA 与常用字符覆盖校验（防方框）
    ├── dev_server.py                本地预览服务器
    └── e2e_*.mjs                    Playwright 端到端测试
```

**放进现有仓库要额外注意的四件事**（因为两站共用一次部署）

| # | 注意点 | 说明 |
| --- | --- | --- |
| 1 | `_headers` 要补两条 | 现有 `/js/*`、`/css/*` 是路径前缀规则，**不会覆盖** `/en/js/`、`/en/css/`，必须为新站各加一条同样的缓存规则 |
| 2 | COOP/COEP 保持不动 | 现有 `/*` 的 COOP/COEP 会一并作用到 `/en/`；英语站不需要它，但也**不受影响**（Service Worker、MediaRecorder、Web Audio 在 COEP 下都正常），所以不用改 |
| 3 | 发版清单要扩 | README 里那三条版本号规则扩成五条：加上 `/en/index.html` 的 `?v=` 与 `/en/js/...` 的 `const V` |
| 4 | 一次部署发布两站 | `wrangler pages deploy .` 会把两站一起发（vendor 那 25MB 本来就在仓库里，不额外增加成本）；D1 用同一个 `mian-db`，表名互不冲突 |

---

## 11. 内容生产管线与自测

**参考项目最值钱的资产不是代码，而是「内容可以被安全地大规模生产」的管线。** 英语站直接复制这套：

```
① 编内容      tools/words.csv（Excel 也能编辑：词 / 音标 / 中文 / 主题 / 级别 / 例句）
                   ↓  python3 tools/gen_words.py
② 生成数据    data/words-*.js     （自动补 id、去重、校验音标格式、检查例句包含该词）
                   ↓  python3 tools/gen_audio.py
③ 生成音频    assets/audio/*.opus / *.m4a / *.json
                   ↓  python3 tools/check_content.py && python3 tools/check_audio.py
④ 校验入库    ❌ 任何一条不合格 → 直接报错、不写文件（照搬 gen_exercises.py 的做法）
                   ↓  node tools/e2e_*.mjs
⑤ 端到端      Playwright 真机跑一遍全流程
```

**check_content.py 的断言清单**（对应参考项目的 check_lessons.py）

| # | 断言 |
| --- | --- |
| 1 | 每个词条有 id / word / ipa / zh / theme / audio 索引，且 id 不重复 |
| 2 | 音标字段用斜杠包裹且只含合法 IPA 字符 |
| 3 | 每道题的答案**唯一**（选项不重复、干扰项不等于正确答案） |
| 4 | 关卡引用的每个 word id 都真实存在（防止改名漏改） |
| 5 | 每个词的例句**包含该词**（大小写与变形要显式声明） |
| 6 | 每个词有对应图标文件（或明确的 emoji 兜底） |
| 7 | 绘本每一页的句子都在音频索引里 |
| 8 | 音频索引与音频文件一一对应、无孤儿条目 |

**e2e_*.mjs 的断言清单**（对应参考项目的 e2e_learn_v2.mjs 61 项）

| 文件 | 覆盖 |
| --- | --- |
| e2e_stage.mjs | 走完一个主题岛：听→说→读→写→玩→结算→经验到账→记忆盒入库（20+ 断言） |
| e2e_speech.mjs | 录音权限被拒绝 / 浏览器不支持 MediaRecorder 时的**降级路径** |
| e2e_offline.mjs | 断网后打开已缓存的主题仍可做题、进度不丢 |
| e2e_sync.mjs | 两个浏览器上下文：换设备登录同步码 → 进度与记忆盒一致（照搬 test_cloud_sync.mjs） |
| e2e_a11y.mjs | 键盘走完一道题、焦点可见、对比度、aria-live 播报 |

**本地预览**：python3 tools/dev_server.py 8799（英语站**不需要** COOP/COEP，比参考项目简单；但要带 Cache-Control: no-store，防止调试时读到旧文件）。

---

## 12. 性能、无障碍与儿童安全

### 12.1 性能预算（4G 网络 / iPad 2019 为基准机）

| 指标 | 目标 | 手段 |
| --- | --- | --- |
| 首屏可交互 | < 2s | 首屏只加载地图 + 设计系统（预计 < 180KB gzip），词库/音频按需 |
| 关卡进入 | < 300ms | 主题包预取；音频精灵一次请求 |
| 播放延迟 | < 80ms | 音频精灵 + currentTime 定位（无网络往返） |
| 图片 | SVG 为主 | OpenMoji 子集 1–3KB/个，按主题分包懒加载 |
| 字体 | 子集化 | 拉丁子集 < 60KB/字重；中文只打包站内出现的字 |

### 12.2 无障碍与低龄适配

- **触控** ≥ 64×64px（主按钮 ≥ 88px 高）；按钮间距 ≥ 16px（防误触）
- **不依赖颜色**：对错同时用 ✓/✗ 图标 + 文字 + 音效三重编码（色盲友好）
- **朗读优先**：题干、选项、按钮都有小喇叭；不识字的孩子可独立完成听力类关卡
- **键盘可达**：Tab 顺序合理、焦点环清晰、Esc 退出关卡、Enter 播放下一个音（大孩子与家长陪读用）
- **aria-live**：判题结果与结算用 aria-live="polite" 播报（屏幕阅读器兼容）
- **护眼**：夜间模式自动切换（19:00 后）+ 手动覆盖；连续 20 分钟弹一次「看看窗外 20 秒 🐼」；学习字号三档可调（沿用参考项目的护眼大字号）

### 12.3 儿童安全与合规

| 红线 | 做法 |
| --- | --- |
| 不收集个人信息 | 身份 = 匿名同步码（只存 SHA-256 hash + 服务端 pepper），无姓名 / 邮箱 / 手机号 / 生日 |
| 不投放广告、不做社交、不做排行攀比 | 完全没有 UGC 与陌生人交互面 |
| 录音不上传 | 默认本机 IndexedDB；云端评测需家长逐次授权并明示「录音会上传给微软评分」 |
| 防沉迷 | 每日目标 + 时长上限（家长可设）+ 到点温和提示休息（不是强制踢出） |
| 家长可见 | 所有数据家长都能在自己手机上看到（透明，不偷偷收集） |
---

## 13. 里程碑与验收

| 阶段 | 交付 | 验收标准 | 预估 |
| --- | --- | --- | --- |
| **M0 骨架** | 仓库结构 · 设计系统（色板/字体/组件）· 地图首页 · hash 路由 · 音效合成 · dev_server · 部署管道 | mian.lihaiyang.net/en/ 能打开地图首页；字体/图标自托管加载正常；两站 e2e 都不回归 | 1 天 |
| **M1 最小闭环** ⭐ | 生活岛 2 个主题（80 词，Grade 2）· 五小关全部题型 · 音频管线（音频精灵）· **本地节奏评测 + 三明治回放** · 记忆盒 · 成长档案 · 家长报告 v1 | 孩子能独立走完一座岛并看到经验到账；次日回来能看到到期复习卡；录音被拒也有降级路径；e2e_stage.mjs 全绿 | 3–5 天 |
| **M2 拼读与绘本** | 字母岛 26 关 · 拼读岛 30 关 · 拼读机 · 点读绘本引擎 + 6 本样书 | 拼读拆音动画可用；绘本可点读、可整本跟读 | 3–5 天 |
| **M3 游戏与挑战** | 5 个小游戏 · 句子岛 100 句型 · 模拟卷（含成绩单与错题重做） | 小游戏与关卡共用词库；模拟卷能出分并存档 | 2–3 天 |
| **M4 离线与云同步** | Service Worker 离线 · PWA 安装 · D1 同步（含 srs_cards / daily）· 分享页 | e2e_sync.mjs + e2e_offline.mjs 全绿；iPad 加到主屏后断网可学 | 2 天 |
| **M5 内容扩充与分档** | 词库 600+（Grade 1/2/3 三档）· **最小对立对听辨题库** · 绘本 60 本 · 挑战岛 20 套 | check_content.py / check_audio.py 全绿；三档都能从地图进入 | 持续 |

> **开发顺序的哲学**（照搬参考项目的成功经验）：**先把一个主题的两个词表做到「能用」，再横向铺量**。1318 道题是靠管线生成的，不是手写出来的；英语站也一样 —— **管线和引擎先跑通，内容是水到渠成的**。

---

## 14. 风险与对策

| 风险 | 影响 | 对策 |
| --- | --- | --- |
| **TTS 在部分设备无声** | 靠听的产品直接废掉 | 自托管音频精灵为唯一真相源（构建期合成，不依赖运行时 TTS）；三级降级链；首次进入做「能听到声音吗？」自检 |
| **本地评测给不出音准，孩子读错了也不知道** | 家长觉得「这功能没用」 | §8.2 的四条补偿：三明治回放（孩子自己听差别）+ 最小对立对听辨（可自动判分）+ 口型提示 + 家长人工确认；界面只给可验证的信号，**绝不给假的音准分** |
| **孩子三分钟热度** | 留存差 | SRS 到期召回 + 连续打卡 + 宠物 + 每日任务 + 家长报告；每日路径 ≤ 15 分钟且**进门就能开始**（零决策成本） |
| **录音权限被拒 / 不支持 MediaRecorder** | 口语关不可用 | 三级降级：① 有权限 → 评测 + 回放；② 无权限 → 只跟读、只听示范音，不打分；③ 完全不支持 → 口语关自动换成听辨题（e2e_speech.mjs 专门测这三条路径） |
| **内容量太大做不完** | 烂尾 | M1 只做 2 个主题 80 词；管线先行；之后每周按 CSV 增量补词 |
| **第三方资源被拦 / 版权纠纷** | 打不开 / 下架 | 全自托管；字体 OFL、图标按 CC 授权（页脚署名）、音频自产、词表自编 |
| **CDN 缓存旧版本** | 新 HTML + 旧 CSS 错版 | 沿用版本号机制（?v= + const V），发版清单写进 README |
| **D1 免费额度被内容查询烧掉** | 服务中断 | 内容全静态、不入库；同步只传增量；rate 表限流 |
| **共用仓库互相影响** | 两站一起发布、一起回滚 | 英语站全部代码收在 `/en/` 下自成一体；共用文件只有 `_headers` / `wrangler.toml` / `functions/api/_utils.js`，改共用文件前先跑两站的 e2e |
| **儿童隐私** | 合规风险 | 匿名身份、录音不出本机、无社交无广告、家长全透明可见 |

---

## 15. 决策记录与剩余待定

### 15.1 已拍板（v1.1）

| # | 议题 | 结论 | 对方案做了什么改动 |
| --- | --- | --- | --- |
| 1 | 年龄段 | **三档都做，首版做 6–8 岁（Grade 2）**，再向 4–6 岁与 8–10 岁扩 | 词条加 `grade` 字段；地图左上角一键切档；内容先做中档（见 §3 末） |
| 2 | 发音评测 | **只做本地，不接云端** | §8.2 整节重写为「本地节奏评测 + 四条补偿设计」；数据模型删掉 recordings 表；录音永不出本机 |
| 3 | 内容来源 | **自编主题 + 标注课标级别** | 主题词库自己编排，每词标课标一级/二级；不被任何教材版本绑死 |
| 4 | 部署 | **现有仓库子目录 /en/** | 目录结构改为 `en/` 子目录；共用 `_headers` / `wrangler.toml` / `functions/api/_utils.js`；需补缓存规则与发版清单（见 §10 末） |

### 15.2 剩余待定（都不阻塞 M0，做到那一步再定）

| # | 议题 | 我的默认选择 | 最晚什么时候定 |
| --- | --- | --- | --- |
| 1 | 名字与 IP | 「萌语岛 English Island」，延续熊猫 IP（与萌码 Python 形成萌系列） | M0 建目录前（影响目录名、页面标题、图标） |
| 2 | 家长端形态 | 站内页面 + 可分享的只读周报链接（微信推送需要公众号，成本高，后置） | M1 做家长报告前 |
| 3 | 首个主题 | food（食物）—— 词最具体、图最好认、孩子最有表达欲 | M1 开工时 |
| 4 | 首个绘本 | My Cat（8 页，全是 CVC 短元音词，正好承接拼读岛） | M2 开工时 |
| 5 | 独立域名 | 先沿用 mian.lihaiyang.net/en/；将来要独立再迁移（代码与路径无关，迁移成本很低） | M4 上线前 |


## 附：与参考项目的资产复用清单

| 参考项目里的东西 | 英语站怎么用 |
| --- | --- |
| schema.sql 的多租户 + rev + 墓碑设计 | 直接照搬表结构约定，增补 srs_cards / daily |
| functions/api/_utils.js（sha256 / genCode / rateLimit / authAccount） | 几乎原样复用（同步码字母表已去掉易混字符，对家长抄写很友好） |
| js/cloud.js 的增量同步 + 3 秒防抖 + 时钟偏移处理 | 复用同步策略，替换 payload 收集器 |
| js/progress.js 的等级 / 徽章 / 奖牌 / 每日任务判定式设计 | 复用架构，换一套英语主题的称号与徽章池 |
| js/audio-confetti.js 的 Web Audio 合成音效 | 直接复用并扩充（盖章、翻卡、升级琶音） |
| vendor/confetti.min.js | 复用（撒花动效） |
| tools/check_lessons.py · gen_exercises.py 的「生成即校验」方法论 | 演化为 check_content.py / gen_audio.py |
| tools/dev_server.py | 直接用（同仓库同根目录，自带的 COOP/COEP 对英语站无害） |
| tools/e2e_learn_v2.mjs 的 Playwright 断言写法 | 复用测试骨架 |
| README 里的「版本号 + 缓存」血泪教训 | 原样继承为发版清单 |

---

*方案结束（v1.1，已按 §15.1 的四项拍板结论修订）。下一步 M0：在 /en/ 下搭骨架 + 落设计系统 + 部署一个能打开的地图首页。*
